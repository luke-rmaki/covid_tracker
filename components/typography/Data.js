import styled from "styled-components";
import { motion } from "framer-motion";

const Data = styled(motion.p)`
  font-family: "Raleway", sans-serif;
  color: var(--red);
  text-align: center;
`;

export default Data;
